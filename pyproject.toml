[project]
name = "token_validator"
version = "0.1.0"
description = "A client library for accessing FastAPI"
authors = []
requires-python = "~=3.9"
readme = "README.md"
dependencies = [
    "httpx>=0.23.0,<0.29.0",
    "attrs>=22.2.0",
    "python-dateutil>=2.8.0,<3",
]

[tool.hatch.build.targets.sdist]
include = [
    "token_validator",
    "CHANGELOG.md",
    "token_validator/py.typed",
]

[tool.hatch.build.targets.wheel]
include = [
    "token_validator",
    "CHANGELOG.md",
    "token_validator/py.typed",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["F", "I", "UP"]
